# スタエフ AIアシスタント

stand.fm配信者のための音声コンテンツ自動化ツール

## 概要

音声ファイルをアップロードするだけで、以下を自動生成します：
- 📝 整形済み文字起こし
- 📋 要約
- 🏷️ タイトル候補（3つ）
- 🎙️ stand.fm概要欄
- 🐦 X投稿文

## セットアップ

### 1. 依存関係のインストール

```bash
npm install
```

### 2. 環境変数の設定

`.env.example` をコピーして `.env.local` を作成し、以下を設定：

```bash
cp .env.example .env.local
```

必要なAPIキー：
- **Supabase**: [supabase.com](https://supabase.com) でプロジェクト作成
- **OpenAI**: [platform.openai.com](https://platform.openai.com) でAPIキー取得
- **Google AI**: [aistudio.google.com](https://aistudio.google.com) でAPIキー取得

### 3. 開発サーバー起動

```bash
npm run dev
```

ブラウザで [http://localhost:3000](http://localhost:3000) を開く

## 使い方

1. 🎙️ **音声ファイルを選択**（mp3, m4a, wav, webm対応）
2. ✨ **生成スタイルを選択**（標準/カジュアル/丁寧/短め）
3. 📋 **生成結果をコピー** → stand.fm / X に投稿

## 技術スタック

- **Frontend**: Next.js 14 (App Router) + TypeScript
- **Styling**: Tailwind CSS
- **Backend**: Supabase (Auth/DB/Storage)
- **AI**: OpenAI Whisper, Google Gemini

## フォルダ構成

```
src/
├── app/              # ページ
│   ├── page.tsx      # メイン（アップロード）
│   ├── library/      # 履歴一覧
│   └── settings/     # 設定
├── components/       # UIコンポーネント
└── lib/              # ユーティリティ
    └── supabase/     # Supabase設定
```

## ライセンス

MIT License
